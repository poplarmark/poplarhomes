// Get a reference to the trigger link
const trigger_nevada = document.getElementById("trigger-nevada");
const trigger_colorado = document.getElementById("trigger-colorado");
const trigger_texas = document.getElementById("trigger-texas");
const trigger_arizona = document.getElementById("trigger-arizona");
const trigger_california = document.getElementById("trigger-california");
const trigger_oregon = document.getElementById("trigger-oregon");
const trigger_washington = document.getElementById("trigger-washington");
const trigger_missouri = document.getElementById("trigger-missouri");
const trigger_wisconsin = document.getElementById("trigger-wisconsin");
const trigger_ohio = document.getElementById("trigger-ohio");
const trigger_illinois = document.getElementById("trigger-illinois");
const trigger_michigan = document.getElementById("trigger-michigan");
const trigger_indiana = document.getElementById("trigger-indiana");
const trigger_maryland = document.getElementById("trigger-maryland");
const trigger_tennessee = document.getElementById("trigger-tennessee");
const trigger_carolina_north = document.getElementById("trigger-carolina-north");
const trigger_carolina_south = document.getElementById("trigger-carolina-south");

// Add a click event listener to the trigger link
trigger_nevada.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_nevada");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_colorado.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_colorado");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_texas.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_texas");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_arizona.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_arizona");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_california.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_california");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_oregon.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_oregon");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_washington.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_washington");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_missouri.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_missouri");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_wisconsin.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_wisconsin");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_ohio.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_ohio");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_illinois.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_illinois");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_michigan.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_michigan");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_indiana.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_indiana");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_maryland.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_maryland");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_tennessee.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_tennessee");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_carolina_north.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_carolina-north");
        element.style.display = "block"; // Change to "inline" if needed
});
// Add a click event listener to the trigger link
trigger_carolina_south.addEventListener("click", function (event) {
    event.preventDefault(); // Prevent the link from navigating

    // Get all elements with the class "target-element"
    let element = document.getElementById("modal_carolina-south");
        element.style.display = "block"; // Change to "inline" if needed
});